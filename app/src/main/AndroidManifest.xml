<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.todaysmenu">

    <application
        android:allowBackup="true"
        android:icon="@drawable/baker"
        android:label="@string/app_name_ko"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".CustomMainActivity"
            android:label="@string/title_activity_custom_main"></activity>
        <activity android:name=".JoinInfoActivity" />
        <activity android:name=".JoinActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SellerJoinActivity" />
        <activity android:name=".SellerMenuActivity" />
        <activity android:name=".SellerMainActivity" />
        <activity android:name=".SellerInfoActivity" />
        <activity android:name=".SellerMenuDeleteActivity" />
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name_ko">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
    </application>
</manifest>